#version 460
layout(local_size_x = 1, local_size_y = 1) in;
layout(binding = 0, rgba32f) uniform image2D outImage;

void main()
{
    vec2 uv = gl_GlobalInvocationID.xy / (vec2(gl_NumWorkGroups) * vec2(gl_WorkGroupSize));
    imageStore(outImage, ivec2(gl_GlobalInvocationID.xy), vec4(uv, 0, 1));
}
